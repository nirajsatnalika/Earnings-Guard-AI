import { DeleteOutline, DownloadOutlined, VisibilityOutlined } from '@mui/icons-material'
import { Box, Button, Card, CardContent, Chip, Grid, IconButton, Stack, Table, TableBody, TableCell, TableContainer, TableHead, TableRow, Typography } from '@mui/material'
import { useState } from 'react'
import { ConfirmDialog, EmptyState, SuccessDialog } from '../components/feedback/FeedbackComponents'
const stats = [['Total reports', '24'], ['Generated this month', '08'], ['PDF downloads', '142'], ['Excel downloads', '67']]
const initialRows = [{ id: 1, company: 'Infosys Ltd.', type: 'Forensic Analysis', date: '22 Jul 2026', status: 'Ready' }, { id: 2, company: 'TCS', type: 'Forensic Analysis', date: '18 Jul 2026', status: 'Ready' }, { id: 3, company: 'HCL Technologies', type: 'Quarterly Review', date: '12 Jul 2026', status: 'Ready' }, { id: 4, company: 'Wipro', type: 'Forensic Analysis', date: '08 Jul 2026', status: 'Ready' }]
export function Reports() { const [rows, setRows] = useState(initialRows); const [deleteId, setDeleteId] = useState<number | null>(null); const [success, setSuccess] = useState(false); return <Box sx={{ p: { xs: 2, md: 4 }, maxWidth: 1400, mx: 'auto' }}><Stack direction={{ xs: 'column', sm: 'row' }} justifyContent="space-between" spacing={2} sx={{ mb: 3.5 }}><Box><Typography variant="h1">Reports</Typography><Typography color="text.secondary" sx={{ mt: .5 }}>Manage and download your generated forensic intelligence reports.</Typography></Box><Button variant="contained" onClick={() => setSuccess(true)}>Generate report</Button></Stack><Grid container spacing={2} sx={{ mb: 2 }}>{stats.map(([label, value]) => <Grid size={{ xs: 6, md: 3 }} key={label}><Card><CardContent sx={{ p: 2.5 }}><Typography variant="body2" color="text.secondary">{label}</Typography><Typography variant="h2" sx={{ mt: 1 }}>{value}</Typography></CardContent></Card></Grid>)}</Grid>{rows.length ? <Card><CardContent sx={{ p: 0 }}><Box sx={{ p: 2.5, pb: 1.5 }}><Typography variant="h3">Report library</Typography></Box><TableContainer><Table size="small"><TableHead><TableRow>{['COMPANY', 'REPORT TYPE', 'GENERATED ON', 'STATUS', 'ACTIONS'].map((head) => <TableCell key={head}>{head}</TableCell>)}</TableRow></TableHead><TableBody>{rows.map((row) => <TableRow key={row.id}><TableCell sx={{ fontWeight: 700 }}>{row.company}</TableCell><TableCell>{row.type}</TableCell><TableCell>{row.date}</TableCell><TableCell><Chip label={row.status} size="small" color="success" /></TableCell><TableCell><Stack direction="row"><IconButton size="small"><VisibilityOutlined fontSize="small" /></IconButton><IconButton size="small" onClick={() => setSuccess(true)}><DownloadOutlined fontSize="small" /></IconButton><IconButton size="small" color="error" onClick={() => setDeleteId(row.id)}><DeleteOutline fontSize="small" /></IconButton></Stack></TableCell></TableRow>)}</TableBody></Table></TableContainer></CardContent></Card> : <EmptyState variant="reports" />}<ConfirmDialog open={deleteId !== null} title="Delete report?" message="This report will be permanently removed from the library." onCancel={() => setDeleteId(null)} onConfirm={() => { setRows((current) => current.filter((row) => row.id !== deleteId)); setDeleteId(null) }} /><SuccessDialog open={success} title="Report generated" message="Your report is ready to view and download." onClose={() => setSuccess(false)} /></Box> }
