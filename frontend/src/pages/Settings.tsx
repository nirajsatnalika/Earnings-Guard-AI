import { Box, Button, Card, CardContent, FormControl, FormControlLabel, InputLabel, MenuItem, Radio, RadioGroup, Select, Stack, Switch, TextField, Typography } from '@mui/material'
import { useState } from 'react'
import { SuccessDialog } from '../components/feedback/FeedbackComponents'
export function Settings() { const [themeChoice, setThemeChoice] = useState('system'); const [saved, setSaved] = useState(false); return <Box sx={{ p: { xs: 2, md: 4 }, maxWidth: 1000, mx: 'auto' }}><Typography variant="h1">Settings</Typography><Typography color="text.secondary" sx={{ mt: .5, mb: 3.5 }}>Manage your profile, AI preferences, and notification settings.</Typography><Stack spacing={2}><Section title="Profile" description="Your personal and organization details."><Box sx={{ display: 'grid', gridTemplateColumns: { xs: '1fr', sm: '1fr 1fr' }, gap: 2 }}><TextField label="Name" defaultValue="Alex Morgan" /><TextField label="Email" defaultValue="alex.morgan@example.com" /><TextField label="Organization" defaultValue="Northstar Capital" /></Box></Section><Section title="Theme" description="Choose how EarningsGuard appears on your devices."><RadioGroup row value={themeChoice} onChange={(event) => setThemeChoice(event.target.value)}>{['light', 'dark', 'system'].map((choice) => <FormControlLabel key={choice} value={choice} control={<Radio />} label={choice[0].toUpperCase() + choice.slice(1)} />)}</RadioGroup></Section><Section title="AI settings" description="Configure the model and language used for analysis summaries."><Box sx={{ display: 'grid', gridTemplateColumns: { xs: '1fr', sm: '1fr 1fr' }, gap: 2 }}><FormControl><InputLabel>Model</InputLabel><Select label="Model" defaultValue="ollama"><MenuItem value="ollama">Ollama</MenuItem></Select></FormControl><FormControl><InputLabel>Language</InputLabel><Select label="Language" defaultValue="english"><MenuItem value="english">English</MenuItem></Select></FormControl></Box></Section><Section title="Notifications" description="Choose where to receive important analysis updates."><Stack><FormControlLabel control={<Switch defaultChecked />} label="Email notifications" /><FormControlLabel control={<Switch defaultChecked />} label="Desktop notifications" /></Stack></Section><Stack direction="row" justifyContent="flex-end" spacing={1}><Button>Reset</Button><Button variant="contained" onClick={() => setSaved(true)}>Save changes</Button></Stack></Stack><SuccessDialog open={saved} title="Settings saved" message="Your preferences have been updated for this workspace." onClose={() => setSaved(false)} /></Box> }
function Section({ title, description, children }: { title: string; description: string; children: React.ReactNode }) { return <Card><CardContent sx={{ p: { xs: 2.5, md: 3 } }}><Typography variant="h3">{title}</Typography><Typography variant="body2" color="text.secondary" sx={{ mt: .5, mb: 2.5 }}>{description}</Typography>{children}</CardContent></Card> }
